<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PortfolioMobile1.ViewModels"
             x:Class="PortfolioMobile1.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image x:Name="myfaceImage"
                Source="myface.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="My face" />

            <Label x:Name="fullNameLabel"
                Text="{Binding FullName}"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <ListView ItemsSource="{Binding MyStackList}"/>


            
            


            <Button
                x:Name="getJsonButton"
                Text="GET" 
                SemanticProperties.Hint="Fetches data from apache server"
                Clicked="OnGetJsonButtonClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
